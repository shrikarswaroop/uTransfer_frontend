import { createContext } from "react";

const RefreshDeviceDisplayContext = createContext({refreshDeviceDisplay : 0, setRefreshDeviceDisplay:(state)=>{}});
export default RefreshDeviceDisplayContext;
